<div 
    id="sectionNav"
    class="w-full px-0 lg:px-10 lg:container bg-blue-congress" 
    x-effect="$el.setAttribute('aria-expanded',  $store.burgeropen); $el.setAttribute('aria-hidden', ! $store.burgeropen)"
>
    <div class="flex content-start mb-0">
        <div class="flex w-full">
            <span id="sectionNavHeadline" class="hidden">Bereichsnavigation</span>
            {{#with this.sectionNavigationSSILinks}}
                <ul class="flex flex-col items-center w-full text-left text-white list-none lg:flex-row lg:h-auto lg:mt-1">
                    {{#each this}}   
                        {{#if (isStorybook)}}  
                            {{> components/Header/SectionNavigation/SectionNavigationItem }}   
                        {{else}}
                            {{{this}}}
                        {{/if}}
                    {{/each}} 
                </ul>
            {{/with}}
        </div>
    </div>
</div> 