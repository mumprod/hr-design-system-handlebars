import {
  ArgsTable,
  Meta,
  Story,
  Canvas,
  Preview,
} from "@storybook/addon-docs/blocks";

import item from "./ServiceNavigationItem.hbs";

import JsonData from './serviceIconData.json';


<Meta
  title="Komponenten/Menu/ServiceNavigationItem"
  argTypes={{
    label: {
      control: "text",
      description: "Label Text",
    },
    icon: {
      control: "text",
      description: "Icon",
    },
  }}
/>


export const Template = ({ ...args }) => {
  return item({ ...args });
}; 



export const OneItem = Template.bind({});

# Button

Ein toller Einleitungstext für unsere `Label` Komponente:

<Preview withToolbar>
  <Story
    name="Wetter"
    args={{
      label: "Wetter",
      icon: "wetter"
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<ArgsTable story="Wetter" />

## Teaser-Heading Sans serif large

<Canvas>
  <Story
    name="Verkehr"
    args={{
      label: "Verkehr",
      icon: "verkehr"
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Verkehr mit Flyout"
    args={{
      label: "Verkehr",
      icon: "verkehr",
      flyout: {
        columns: [
          { items: [{"title": "item1.1"},{"title": "item1.2"},{"title": "item1.3"},{"title": "item1.4"}] },
          { items: [{"title": "item2.1"},{"title": "item2.2"}] }
        ]
      },      
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Verkehr mit Flyout from File"
    args= {JsonData}
    
  >
    {Template.bind({})}
  </Story>
</Canvas>

