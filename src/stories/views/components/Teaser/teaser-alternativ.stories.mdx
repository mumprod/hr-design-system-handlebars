import { ArgsTable, Meta, Story, Canvas, Preview } from '@storybook/addon-docs'
import 'core-js/actual/structured-clone';
import { addLabel, removeLabel, changeTeaserSize } from './labelHelper';
import { addCommentLink } from './jsonHelper';
import teaser from './Teaser-alternativ.hbs'
import hero_teaser from './data/teaser_standard_xxl_serif.json' 
import xl_teaser from './data/teaser_standard_xl_serif.json'
import lg_teaser from './data/teaser_standard_lg_serif.json'
import md_teaser from './data/teaser_standard_md_serif.json'
import sm_teaser from './data/teaser_standard_sm_serif.json'
import ticker_label from './data/ticker_label.json';
import comments from './data/teaser_commentLink.json';
let hero_teaser_with_label  =  structuredClone(hero_teaser);
addLabel(hero_teaser_with_label, ticker_label.label);
let hero_teaser_with_comments  =  structuredClone(hero_teaser);
addCommentLink(hero_teaser_with_comments, comments.commentLink);
let md_teaser_with_comments  =  structuredClone(md_teaser);
addCommentLink(md_teaser_with_comments, comments.commentLink);
let sm_teaser_with_comments  =  structuredClone(sm_teaser);
addCommentLink(sm_teaser_with_comments, comments.commentLink);

<Meta   title="Komponenten/Teaser/Alternativ"
        parameters={{
            layout: 'fullscreen',
        }}
        argTypes={{
            teasersize: {
                control: {
                    type: 'select',
                    options: ['xxl','xl', 'lg', 'md', 'sm'],
                },
                description: 'Teaser Größe',
                table: {
                    defaultValue: {
                        summary: 'xxl',
                    },
                },
            }
        }} 
        decorators={[
            (Story) => {
                return `<div class="grid grid-page"><div class="grid grid-cols-12 py-6 col-full gap-x-6 gap-y-6 sm:px-8 sm:col-main">  
                ${Story()} 
                </div></div>`
            },
        ]}
        />

export const Template = (args, { globals: { customConditionalToolbar } }) => {
    // You can either use a function to create DOM elements or use a plain html string!
    // return `<div>${label}</div>`;
    let brand =
        undefined !== customConditionalToolbar ? customConditionalToolbar['brands'] : 'hessenschau'
    return teaser({ brand, ...args })
}

# Header

Ein toller Einleitungstext für unsere `Teaser` Komponente:

<Preview withToolbar>
    <Story  name="Alternativ Hero" args={hero_teaser_with_comments}>
        {Template.bind({})} 
    </Story>
    <Story name="Alternativ Hero with Label" args={hero_teaser_with_label}>
        {Template.bind({})} 
    </Story>   
    <Story name="Alternativ XL" args={xl_teaser}>
        {Template.bind({})}
    </Story>  
    <Story name="Alternativ LG" args={lg_teaser}>
        {Template.bind({})}
    </Story>
    <Story name="Alternativ MD" args={md_teaser_with_comments}>
        {Template.bind({})}
    </Story>   
    <Story name="Alternativ SM" args={sm_teaser_with_comments}>
        {Template.bind({})}
    </Story>   
   
</Preview>
