<button class="block border-0 ml-4 absolute left-0 min-w-3 min-h-3 top-1/2 js-ns-button -left">
                    {{> components/base/image/icon _icon="arrow-left" _iconmap="icons" _addClass="w-5 h-5 fill-current" }}
                </button>
                <button class="block border-0 mr-4 absolute right-0 min-w-3 min-h-3 top-1/2 js-ns-button -right">
                    {{> components/base/image/icon _icon="arrow-right" _iconmap="icons" _addClass="w-5 h-5 fill-current" }}
                </button>
                <div class="flex relative max-w-100vw ml-14 mr-14 overflow-hidden box-border border-teal-600">
                    <div class="flex mb-0 overflow-x-auto overflow-y-hidden whitespace-nowrap scroll-smooth js-ns-scroll -horizontal">
                {{#each eventCalendar.eventCalendarMonth.months}}
                <div class="inline-block items-center justify-center border-teal-600 border-l ml-4 mr-4 js-ns-month {{#if
                            this.isCurrentMonth}} -currentMonth{{/if}}{{#if
                            this.hasFutureEventsFromNow}} -selected{{/if}}{{#unless
                            this.hasFutureEventsFromNow}} js-fr-reload-trigger{{/unless}}">
                    {{#if ../_excerpt}}
                    <span class="block font-headingSerif font-bold w-24 sticky left-0 px-2 py-3 ">{{this.monthName}} {{this.year}}</span>
                     {{else}}
                     {{#if this.hasDays}}
                                <a href="{{this.contentUrl}}" class="block font-headingSerif font-bold w-24 sticky left-0 px-2 py-3 js-fr-trigger js-noStateManager{{#if
                                           this.hasFutureEventsFromNow}} -selected{{/if}}" data-prop-crit="{{this.date.yearMonth}}" data-prop-filterer='{"crit":"{{this.date.yearMonth}}", "strat":"hoswid"}'>{{this.monthName}} {{this.year}}</a>
                            {{else}}
                                <span class="block font-headingSerif font-bold w-24 sticky left-0 px-2 py-3 ">{{this.monthName}} {{this.year}}</span>
                            {{/if}}
                        {{/if}}
                        <ul class="flex">
                        {{#each this.days}}
                <li class="inline-block h-full group cursor-pointer js-ns-item{{#if
                                        this.isStartOfWeek}} -startOfWeek{{/if}}">
                       {{> components/event/event_calendar_instant}}
                </li>
                {{/each}}
                </ul>
                </div>
                    {{/each}}
                    </div>