{{~#with this.logicItem~}}
    {{~#with this.includeModel~}}
        {{~#if (isStorybook)~}}
            {{~> components/base/load_dynamic _templatePath=../includePath ~}}
        {{~else~}}
            {{~> (lookup ../. 'includePath')~}}
        {{~/if~}}
    {{~/with~}}
{{~/with~}}