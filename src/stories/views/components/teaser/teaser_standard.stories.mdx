import { ArgsTable, Meta, Story, Canvas, Preview } from '@storybook/addon-docs'
import teaser from './teaser_standard.hbs'
import xxlTeaserSerifWithLabel from './fixtures/teaser_standard_xxl_serif_label.json'
import xxlTeaserSerifWithComments from './fixtures/teaser_standard_xxl_serif_comments.json'
import xxlTeaserSerif from './fixtures/teaser_standard_xxl_serif.json'
import xlTeaserSerif from './fixtures/teaser_standard_xl_serif.json'
import lgTeaserSerif from './fixtures/teaser_standard_lg_serif.json'
import mdTeaserSerif from './fixtures/teaser_standard_md_serif.json'
import smTeaserSerif from './fixtures/teaser_standard_sm_serif.json'

<Meta
    title="Komponenten/Teaser/Standard"
    parameters={{
        layout: 'fullscreen',
    }}
    argTypes={{
        teasersize: {
            control: {
                type: 'select',
                options: ['xxl', 'xl', 'lg', 'md', 'sm'],
            },
            description: 'Teaser Größe',
            table: {
                defaultValue: {
                    summary: 'xxl',
                },
            },
        },
    }}
    decorators={[
        (Story) => {
            return `<div class="grid grid-page"><div class="grid grid-cols-12 py-6 col-full gap-x-6 gap-y-6 sm:px-8 sm:col-main">  
                ${Story()} 
                </div></div>`
        },
    ]}
/>

export const Template = (args, { globals: { customConditionalToolbar } }) => {
    // You can either use a function to create DOM elements or use a plain html string!
    // return `<div>${label}</div>`;
    let brand =
        undefined !== customConditionalToolbar ? customConditionalToolbar['brands'] : 'hessenschau'
    return teaser({ brand, ...args })
}

# Header

Ein toller Einleitungstext für unsere `Teaser` Komponente:

<Preview withToolbar>
    <Story name="Standard Hero" args={xxlTeaserSerif.logicItem.includeModel}>
        {Template.bind({})}
    </Story>
    <Story name="Standard Hero mit Label" args={xxlTeaserSerifWithLabel.logicItem.includeModel}>
        {Template.bind({})}
    </Story>
    <Story name="Standard Hero Mit Kommentaren" args={xxlTeaserSerifWithComments.logicItem.includeModel}>
        {Template.bind({})}
    </Story>
    <Story name="Standard XL (100%)" args={xlTeaserSerif.logicItem.includeModel}>
        {Template.bind({})}
    </Story>
    <Story name="Standard LG (50%)" args={lgTeaserSerif.logicItem.includeModel}>
        {Template.bind({})}
    </Story>
    <Story name="Standard MD (33%)" args={mdTeaserSerif.logicItem.includeModel}>
        {Template.bind({})}
    </Story>
    <Story name="Standard SM (25%)" args={smTeaserSerif.logicItem.includeModel}>
        {Template.bind({})}
    </Story>
</Preview>
