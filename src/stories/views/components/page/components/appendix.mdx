import { ArgTypes, Meta, Story, Canvas } from '@storybook/blocks'
import * as AppendixStories from './appendix.stories'

<Meta of={AppendixStories} />

# Appendix

## Beschreibung

Im Appendix können folgende Elemente enthalten sein: 
- Programm-Referenz (mit Logo)
- Ortsseiten-links
- Themenseiten-links

## Verwendung

Die Appendix-Komponente wird in handlebars wie folgt eingebaut:

```html
{{> components/page/components/appendix _isArticle=true|false }}
```

<Canvas withToolbar>
    <Story of={AppendixStories.Default} />
</Canvas>

<ArgTypes story="default" />
