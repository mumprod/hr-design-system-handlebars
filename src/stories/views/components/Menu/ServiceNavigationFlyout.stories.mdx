import {
  ArgsTable,
  Meta,
  Story,
  Canvas,
  Preview,
} from "@storybook/addon-docs/blocks";

import flyout from "./ServiceNavigationFlyout.hbs";

import JsonData from './serviceNavigationData.json';

<Meta
  title="Komponenten/Menu/ServiceNavigationFlyout"
  argTypes={{
    columns: {
      control: "array",
      description: "Menü in JSON Format",
    },
    
  }}
/>



export const Template = ({ ...args }) => {
  // You can either use a function to create DOM elements or use a plain html string!
  // return `<div>${label}</div>`;
  return flyout({ ...args });
};

# Button

Ein toller Einleitungstext für unsere `Label` Komponente:

<Preview withToolbar>
  <Story
    name="1Column"
    args={JsonData.serviceNavigationSSILinks.navigationItems[2]}
  >
    {Template.bind({})}
  </Story>
</Preview>

<ArgsTable story="1Column" />

<Canvas>
  <Story
    name="Flyout2Column"
    args={JsonData.serviceNavigationSSILinks.navigationItems[1]}
  >
    {Template.bind({})}
  </Story>
</Canvas>