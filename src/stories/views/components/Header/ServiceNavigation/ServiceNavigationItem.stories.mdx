import {
  ArgsTable,
  Meta,
  Story,
  Canvas,
  Preview,
} from "@storybook/addon-docs/blocks";

import item from "./ServiceNavigationItem.hbs";

import JsonData from '../navigation.json';


<Meta
  title="Komponenten/Header/ServiceNavigation/ServiceNavigationItem"
  argTypes={{
    label: {
      control: "text",
      description: "Label Text",
    },
    icon: {
      control: "text",
      description: "Icon",
    },
  }}
/>


export const Template = ({ ...args }) => {
  return item({ ...args });
}; 



export const OneItem = Template.bind({});

# Button

Ein toller Einleitungstext für unsere `Label` Komponente:

<Preview withToolbar>
  <Story
    name="Wetter it Flyout"
    args={JsonData.serviceNavigationSSILinks[1]}
  >
    {Template.bind({})}
  </Story>
</Preview>

<ArgsTable story="Wetter" />

## Teaser-Heading Sans serif large

<Canvas>
  <Story
    name="Verkehr mit Flyout from File"
    args= {JsonData.serviceNavigationSSILinks[0]}
    
  >
    {Template.bind({})}
  </Story>
</Canvas>

